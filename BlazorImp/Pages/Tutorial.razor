@using BlazorImp.Models

@if (HasVideo)
{
    <VideoComponent/>
}

<NavigationComponent/>

@code {
    public Content Content { get; set; }

    private string Header { get; set; } = "Обучающий материал";

    private bool HasVideo { get; set; } = true;
}
